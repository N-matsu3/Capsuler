<div class="container">
  <div class="row">

      <h4>いいねした一覧</h4>
            <%# if @favorites.item.title? %>
      <div class="col-md-12 mx-auto d-flex">
      <% @favorites.each do |favorite| %>

      <%= favorite.item.title %>
      <%# else %>

    </div>
  </div>
</div>