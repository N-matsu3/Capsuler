<div class="container">
  <div class="row">

    <div class="col-md-12 my-5">
      <div class="col-md-12 my-3"><%= flash[:notice] %></div>
      <div class="col-md-12 mb-5">
        <p>

          Capsulerへようこそ！</br>
          ここではついつい回してしまったカプセルトイ（ガチャガチャ）を公開したり、</br>
          いいね！をしたり、コメントをしたり…</br>
          小さなときめきを共有し合う場所です(o^―^o)</br>

        </p>
      </div>

      <h3 class="col-md-12　mt-5">最近のみんなの投稿</h3>

      <div class="col-md-12 my-3 mx-auto d-flex">
        <% @items.each do |item| %>
        <div class="card mx-3 " style="width: 16rem;">
          <% if item.image.attached? %>
            <%=image_tag item.image, size: "200x200" ,class:'mx-auto' %>
          <% else %>
            <%= image_tag 'no_image', size: "200x200" ,class:'mx-auto' %>
          <% end %>

          <h5 class="card-title mx-auto">
            <%= link_to item_path(item) do %>
              <%= item.title %>
            <% end %>
          </h5>

          <div class="index-rating mx-auto"><p class="card-text"><%= render 'rate', item:item %></p>
          </div>
          <p class="card-text mx-auto">by.<%= item.user.user_name %></p>
          <p class="card-text mx-auto"><%= item.created_at.strftime('%Y/%m/%d/%T') %></p>
        </div>
      <% end %>

      </div>

    </div>
  </div>
</div>

